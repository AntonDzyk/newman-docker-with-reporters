name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: debug
        env:
          VAR1: ${{ secrets.DOCKER_HUB_USERNAME }}
        run: |
          echo "Test variable: ${{env.VAR1}}"
      -
        name: Login to DockerHub
        uses: docker/login-action@v1.12.0
        env:
          USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_HUB_TOKEN }}
        with:
          username: $USERNAME
          password: $PASSWORD
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/newman-docker-with-reporters:latest
